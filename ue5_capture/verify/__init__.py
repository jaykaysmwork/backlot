"""Verification — self-check of capture outputs.

Runs after capture completes. Validates schemas, file integrity, and
coverage goals; emits quality_report.md for humans + _report.json for
machine consumption.
"""
