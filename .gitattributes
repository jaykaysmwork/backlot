# Force LF line endings for shell scripts — prevents CRLF breakage
# when Windows checkout feeds scripts into Linux Docker containers.
*.sh text eol=lf
entrypoint.sh text eol=lf
Dockerfile* text eol=lf
